CREATE TABLE student(SID INTEGER, SNAME CHAR(30)); INSERT INTO student VALUES(1, 'Tom'); INSERT INTO student VALUES(2, 'Jack'); \set SQLTERM / CREATE OR REPLACE FUNCTION return_type RETURNS REFCURSOR AS DECLARE CURSOR c1 FOR SELECT * FROM student; BEGIN OPEN c1; RETURN c1; END; / \set SQLTERM ; call return_type();